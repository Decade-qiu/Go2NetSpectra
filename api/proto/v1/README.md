# Legacy Proto Contracts

`api/proto/v1/` is retained for repository history only.

It is no longer a supported source of truth for runtime contracts. Use
`api/thrift/v1/` for maintained packet, query, and AI contract changes.
