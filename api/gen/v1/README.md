# Legacy Generated Proto Bindings

`api/gen/v1/` is retained for repository history and explicit legacy-rejection
tests only.

It is no longer part of the supported runtime or regeneration workflow. Use
`api/gen/thrift/v1/` for maintained generated bindings.
